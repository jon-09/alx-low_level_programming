#include "main.h"
/**
 * mul - function multiplie two integers
 * @a: input integer
 * @b: input integer
 * Return: returns answer after the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
