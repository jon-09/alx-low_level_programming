#include <stdio.h>
/**
 * main - entry point
 * @argv: the argument
 * @argc: the count
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int c = 0;

	while (c <= argc)
	{
		printf("%s\n", argv[0]);
	
		break;
	}

	return (0);
}
