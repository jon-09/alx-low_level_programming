#include <stdio.h>
/**
 * main - Entry Point
 * My hello, printf task
 * Return: return1 after prog execution
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
