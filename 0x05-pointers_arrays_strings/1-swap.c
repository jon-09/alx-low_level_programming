#include "main.h"
/**
 * swap_int - function swaps the values of two integers
 * @a: pointer to an input that will be updated
 * @b: pointer to an input that will be updated
 * Return: Always 0
 */
void swap_int(int *a, int *b)
{
	int x;

	x = *a;
	*a = *b;
	*b = f;
}
